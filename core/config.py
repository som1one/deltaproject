"""
Назначение модуля: служебная конфигурация, не относящаяся напрямую к env-настройкам.

Примеры того, что сюда кладут:
- константы приложения (лимиты пагинации, имена заголовков);
- карты статус-кодов / сообщений по умолчанию;
- feature flags без секретов.

От settings.py отличается тем, что settings — про переменные окружения,
а config — про стабильные или редко меняющиеся параметры кода.

Пока кода нет — только контракт будущей реализации.
"""
