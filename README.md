реализуй фронтенд тз под стиль 4, только фронт
ТЗ: Платформа агентства блогеров для рекламы товаров маркетплейсов
1. Цель платформы
Создать автоматизированную систему, где:
блогеры привлекают работников по реферальной ссылке
работники ищут продавцов и закрывают сделки
продавцы покупают рекламу у блогеров
система автоматически считает сделки, комиссию и выплаты
Платформа должна минимизировать ручную работу.

2. Типы пользователей
На сайте должно быть 3 типа аккаунтов.
1. Работник (аутричер)
Его задача:
писать продавцам маркетплейсов
находить рекламодателей
создавать сделки

2. Блогер
Его задача:
делать рекламу
привлекать работников по реферальной ссылке

3. Администратор (основатель)
Функции:
контроль всех сделок
подтверждение клиентов
управление выплатами
управление блогерами

3. Регистрация
На сайте должна быть регистрация.
Поля:
имя
email / telegram
пароль
Выбор роли:
1 Блогер
2 Работник

Реферальная система
Каждый блогер получает:
site.com/ref/username
Если работник регистрируется по этой ссылке:
он автоматически привязывается к блогеру

4. Кабинет работника
После входа работник видит:
Статистика
сколько сделок создано
сколько подтверждено
сколько оплачено
сколько заработал

Создать новую сделку
Форма:
ссылка на магазин продавца
название товара
контакт продавца
цена интеграции
выбранный блогер
Кнопка:
Создать сделку
Статус сделки:
Новая
Проверка
Подтверждена
Оплачена
Выполнена

Скрипты сообщений
На странице должен быть раздел:
Скрипты для переписки
Чтобы работник просто копировал текст.

5. Кабинет блогера
Блогер видит:
Статистика
сколько работников пришло
сколько сделок
сколько заработано
Пример:
Работников: 52
Сделок: 184
Доход: 184 000 ₽

Реферальная ссылка
Отдельный блок:
Ваша реферальная ссылка
site.com/ref/username
Кнопка:
Скопировать

Сделки
Блогер видит:
какие сделки он должен выполнить
статус рекламы

6. Админ панель
Должна быть полноценная панель.
Разделы:
Пользователи
список работников
список блогеров
Фильтры:
активность
количество сделок

Сделки
Таблица:
работник
блогер
продавец
цена
статус
Кнопки:
подтвердить
отклонить
отметить как оплачено
завершить

Финансы
Система должна автоматически считать:
доход работников
доход блогеров
доход платформы

7. Финансовая модель в системе
Цена интеграции:
15000₽
Автоматическое распределение:
участник
сумма
блогер
5000
работник
2000
реферал блогера
1000
платформа
8000
Система должна автоматически:
считать баланс
добавлять доход пользователю

8. Баланс пользователя
В каждом кабинете должен быть:
Баланс
Показывает:
доступно к выводу
ожидает подтверждения

Кнопка
Запросить выплату
Админ подтверждает вручную.

9. Уведомления
Система должна уведомлять:
Работника:
сделка подтверждена
сделка оплачена
Блогера:
новая интеграция
Админа:
создана новая сделка

10. Автоматизация
Система должна автоматически:
считать комиссию
привязывать работников к блогерам
считать статистику
вести историю сделок

11. Минимальный дизайн
Простой интерфейс.
Страницы:
Главная
Регистрация
Личный кабинет
FAQ
Контакты

12. Главная страница
Текст:
Платформа рекламы товаров у блогеров.

Работники находят рекламодателей.
Блогеры делают рекламу.
Все зарабатывают.
Кнопки:
Начать зарабатывать
Стать блогером

13. Безопасность
Обязательно:
авторизация
защита API
защита реферальной системы
защита от мультиаккаунтов

14. Технологии (пример)
Можно сделать:
Backend:
Node.js / Django
Frontend:
React / Next.js
Database:
PostgreSQL

15. MVP версия (минимум)
Для запуска нужно только:
1 регистрация 2 реферальная система 3 создание сделок 4 админ панель 5 расчет выплат
Это можно сделать за 2–3 недели.

16. Масштабирование
Потом добавить:
автоматический парсер продавцов
интеграцию Telegram
автоматические выплаты
рейтинг работников


---

## Маркетплейс (поддомен)

Приложение `marketplace/` — отдельный Next.js-сервис для поддомена (напр. `market.<домен>`), порт 3001 локально. Редизайн: две темы (светлая/тёмная, «тихая роскошь»), лендинг на корне, каталог `/catalog`, страница блогера `/bloggers/[id]`, заказы `/orders` и `/orders/[id]`, кабинет блогера `/blogger`.

**Env маркетплейса** (`marketplace/.env.local`):

| Переменная | Значение |
| --- | --- |
| `NEXT_PUBLIC_API_BASE_URL` | URL бэкенда |
| `NEXT_PUBLIC_APP_URL` | публичный URL самого маркетплейса |
| `NEXT_PUBLIC_MAIN_APP_URL` | публичный URL главного сайта (для SSO и ссылок) |

На бэкенде `MARKETPLACE_FRONTEND_URL` должен указывать на публичный URL маркетплейса — по нему валидируется `redirect_uri` SSO.

**Вход блогеров (SSO):** на `/auth/login` вкладка «Я блогер» ведёт на `<главный сайт>/oauth/authorize?redirect_uri=<маркет>/auth/platform/callback`. Главная выдаёт одноразовый код (`POST /auth/platform/authorize`), маркет меняет его на JWT (`POST /auth/platform/exchange`). Профиль блогер редактирует в кабинете главной (`/blogger/profile`).

**Оплата заказов:** реквизиты задаются в админке (`Маркетплейс → Оплата`): карта админа (+СБП), расчётный счёт и ключи ЮKassa (приоритетнее ENV; переключатель включает кнопку онлайн-оплаты у заказчика). Заказчик переводит по реквизитам и жмёт «Я перевёл(а) оплату» (`PATCH /marketplace/orders/{id}/mark-paid`) — админ видит бейдж и подтверждает поступление (`confirm-payment`), заказ уходит в эскроу.

---

## Деплой на Railway (one-click)

В репозитории уже лежат конфиги, чтобы поднять прод за пару минут: `railway.toml`, `Procfile`, `runtime.txt`, `scripts/railway_start.sh`, плюс `frontend/railway.toml` для второго сервиса. На билд/старт оба сервиса собираются нативным `Railpack` без Docker.

### 1. Postgres

Railway → ваш проект → `+ New` → `Database` → `PostgreSQL`. Сервис появится с переменной `DATABASE_URL` вида `postgresql://...`.

### 2. Backend (этот репозиторий)

`+ New` → `GitHub Repo` → `deltaproject`. Root Directory оставьте пустым — это и есть бэкенд.

В сервисе → `Variables` добавьте:

| Переменная | Значение |
| --- | --- |
| `DATABASE_URL` | Reference на Postgres → `DATABASE_URL` (бэкенд сам нормализует драйвер в `+asyncpg`). |
| `JWT_SECRET_KEY` | `python -c "import secrets; print(secrets.token_urlsafe(64))"` |
| `REFRESH_TOKEN_SECRET_KEY` | то же, но другая случайная строка |
| `PAYOUT_CARD_PEPPER` | `secrets.token_urlsafe(32)` |
| `ALLOWED_ORIGINS` | публичный URL фронтенд-сервиса (`https://...up.railway.app`) |
| `APP_ENV` | `production` |
| `ADMIN_BOOTSTRAP_EMAIL`, `ADMIN_BOOTSTRAP_PASSWORD` | (опц.) учётка единственного админа — создастся при первом деплое |

Старт-скрипт `scripts/railway_start.sh` сам прогонит `alembic upgrade head`, при наличии `ADMIN_BOOTSTRAP_*` создаст/обновит администратора и поднимет `uvicorn` на `$PORT`. Healthcheck — `/health`.

> **`PAYOUT_CARD_PEPPER` обязателен в проде.** Это соль (pepper) для SHA-256 отпечатка карты выплаты; PAN в БД не хранится. Если переменная не задана (пустая), эндпоинт сохранения карты выплаты (`POST /me/payout-card`) отвечает `HTTP 503` (ошибка конфигурации, явно отличимая от успеха) и не пишет никаких данных карты. Задайте секрет в **Railway → Variables** (или в `.env` на VPS) до приёма карт.
>
> Сгенерировать: `python -c "import secrets; print(secrets.token_urlsafe(64))"`
>
> CI задаёт временное значение `ci-dummy-pepper` только для тестов; на проде используйте собственный случайный секрет. Скрипты `deploy/bootstrap.sh` и `deploy/bootstrap-root.sh` при создании нового `.env` генерируют `PAYOUT_CARD_PEPPER` автоматически.

### 3. Frontend

`+ New` → `GitHub Repo` → тот же репо. В `Settings → Service` укажите `Root Directory = frontend`. Конфиг `frontend/railway.toml` подцепится автоматически.

`Variables`:

| Переменная | Значение |
| --- | --- |
| `NEXT_PUBLIC_API_BASE_URL` | публичный URL backend-сервиса (`https://...up.railway.app`) |
| `NEXT_PUBLIC_APP_URL` | публичный URL самого фронта (его же домен) |

Билд: `npm ci && npm run build`. Старт: `npm run start -- -p $PORT`.

### 4. Готово

Открываем `NEXT_PUBLIC_APP_URL` — всё работает. Каждый `git push` в `main` запускает новый деплой обоих сервисов: бэкенд гонит миграции и стартует `uvicorn`, фронт пересобирает Next.js.

### Локально

```bash
docker compose up -d postgres
cp .env.example .env             # подставьте секреты
.venv\Scripts\python.exe -m alembic upgrade head
.venv\Scripts\python.exe main.py

cd frontend
cp .env.example .env.local
npm install
npm run dev
```
