"""
Пакет routers: HTTP-маршруты FastAPI (APIRouter).

Планируется разбивка по доменам (users.py, items.py, health.py и т.д.),
подключение роутеров в main.py через app.include_router(...).

Пока кода нет — только контракт будущей реализации.
"""
