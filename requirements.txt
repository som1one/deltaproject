# FastAPI и ASGI-сервер
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Асинхронный PostgreSQL: SQLAlchemy 2 + asyncpg
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0

# Синхронный драйвер для Alembic CLI (postgresql+psycopg://)
psycopg[binary]>=3.2.0

# Миграции схемы (Alembic использует синхронный движок; URL см. alembic.ini / env.py)
alembic>=1.14.0

# Настройки из окружения
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# Валидатор EmailStr (pydantic[email]) — нужен для схем с EmailStr
email-validator>=2.2.0

# JWT (HS256)
PyJWT>=2.9.0
cryptography>=43.0.0

# Rate limiting (in-memory по IP)
slowapi>=0.1.9

# HTTP-клиент (ЮKassa API выплат, Telegram OAuth)
httpx[socks]>=0.27.0

# Тесты
pytest>=8.3.0
pytest-asyncio>=0.24.0
hypothesis>=6.100.0

# Загрузка файлов (UploadFile / multipart-формы)
python-multipart>=0.0.9
