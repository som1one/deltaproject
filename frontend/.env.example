# =====================================================================
# Looney Moon · frontend · environment variables
# =====================================================================
# Заполните в Railway → frontend service → Variables.
# Если деплой делите на два сервиса в одном проекте — задавайте через
# Reference на public-домен бэкенда:
#   NEXT_PUBLIC_API_BASE_URL=https://${{backend.RAILWAY_PUBLIC_DOMAIN}}

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_APP_URL=http://localhost:3000
